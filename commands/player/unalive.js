/**
 * /unalive command
 * Marks a player as dead (requires confirmation reaction)
 */

module.exports = {
  name: 'unalive',
  description: 'Mark yourself as dead',
  async execute(interaction) {
    // TODO: Implement unalive logic with reaction confirmation
    await interaction.reply('Unalive not yet implemented.');
  },
};

